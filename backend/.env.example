# MongoDB Connection String
# For Render deployment, add this as an environment variable in the Render dashboard
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGODB_URI=mongodb://localhost:27017/portfolio

# Server Port
# Render will automatically set this, but you can keep it for local development
PORT=3001

# Node Environment
# Set to 'production' in Render
NODE_ENV=development

# WebSocket Configuration
# Comma-separated list of allowed origins for WebSocket connections
# For Render deployment, add this as an environment variable in the Render dashboard
# Include both your production frontend URL and local development URL
WEBSOCKET_ORIGINS=http://localhost:5173,https://anujkidding-portfolio.vercel.app
